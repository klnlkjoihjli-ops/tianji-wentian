# Claude Code 交接说明

更新时间：2026-06-08

## 本轮目标

将产品收敛为面向普通中文用户的“东方 AI 顾问”：更容易提问、更快得到可信回答、更容易采取行动，同时保留现有黑金天象视觉。

## 已完成

- 首页只直接展示 6 个核心分类，儒家与兵法收入“更多典籍”。
- 首页历史记录默认只展示 2 条，可展开其余记录。
- “简要 / 深度”重新可见，默认简要；两种模式的输出长度和耗时已有明显差异。
- 服务端统一返回 `advisor`、`sources`、`confidence`、`safetyNotice`。
- 结果页新增四层摘要：一句结论、典籍依据、现代解释、今日行动。
- 引用只展示服务端检索结果；无可靠检索时要求模型明确说明，不补造原文。
- 中医、时事、命理场景均增加对应的安全边界或不确定性提示。
- 意图分析、典籍检索和历史读取改为并行。
- 登录改为 8 小时有效的 HMAC 签名 token；密码不再返回浏览器；登录与问答均有限流。
- 移除浏览器端 DeepSeek 调用和重复 API key 逻辑。
- 修复 Three.js 线材质警告；小屏、低并发设备和减少动态效果偏好下自动降低粒子、像素比和帧率。
- 桌面结果宽度约 1040px；390px 手机端单列、无横向溢出，并隐藏日晷和八卦文字装饰。
- 首页视觉改为“知识星球”构图：中央暗金星球缓慢呼吸，名言沿三层椭圆轨道环绕运行。
- 环绕名言转到正面时自动变亮、放大，鼠标悬停时暂停并进一步放大；移动端自动减少名言数量。
- 缩小北斗、二十八宿、五行星及其光晕，移除原先大光斑感，保留精细星图和实时天象运动。
- “问天”等待层改为知识星球推演动画，显示“观象、索典、明理、成言”和分阶段进度。
- 结果页增加低对比度旋转星球水印，保持黑金风格，但不干扰正文阅读。
- “简要 / 深度”只保留在初始首页，结果页不再重复显示。
- 修复多次进入结果页时八卦、北斗材质透明度反复相乘的问题。
- 修复回答后点击“重新问天”，星点和光晕突然变大的问题；现在会恢复场景高亮、五行星缩放和名言轨道状态。

## 关键文件

- `src/lib/auth.ts`：token 签发与校验。
- `src/app/api/auth/route.ts`：登录和登录限流。
- `src/app/api/ask/route.ts`：并行检索、结构化结果、安全提示和问答限流。
- `src/lib/prompts.ts`：八个场景提示词及医疗边界。
- `src/app/page.tsx`：不再向 iframe URL 注入密码。
- `public/shenshu.html`：首页、结果层、认证客户端和动画降级。
- `CODEX_HANDOFF.md`：Codex 与 Claude Code 共用的交接记录；后续修改请继续追加到本文件。

## 2026-06-08 视觉与动画改造细节

主要改动均位于 `public/shenshu.html`：

- CSS 中新增 `.wisdom-planet`、`.planet-meridian`、`.wisdom-quote` 和 `.orbit-guide`，构成首页知识星球与三层名言轨道。
- `initFloatingQuotes()` 已替换为 `initWisdomOrbit()`。新逻辑使用单一 `requestAnimationFrame` 更新名言位置、景深、透明度和缩放，不再为每条名言生成独立随机 keyframes。
- `window._setWisdomPaused(on)` 用于在问天期间暂停名言轨道，返回首页后恢复。不要删除，否则隐藏页面中的动画仍会持续更新。
- Three.js 的北斗、二十八宿、五行星尺寸和光晕透明度已整体降低；八卦与浑天仪线条也同步降噪。
- `window._highlightScene(scene)` 不再依赖球体半径判断星点与光晕。星点缩小后旧判断会失效，导致返回首页时光晕被错误设置为高透明度。
- `baguaGroup` 的基础缩放是 `2.4`。动画循环必须围绕 `2.4` 轻微呼吸，不能重新写成围绕 `1` 缩放。
- `window._setResultMode(on)` 保存并按基础透明度计算八卦与北斗状态，不能使用 `opacity *= ...`，否则多次问天后会不断变暗。
- 等待层新增 `.loader-core` 和 `.loader-oracle`；旧的 `.div-hex` 暂时保留兼容，但已隐藏且 `showLoader()` 不再调用 `animateDivHex()`。
- 结果页通过 `#resultPage::after` 显示缓慢旋转的暗色星球水印。
- `goBack()` 会主动调用 `_highlightScene(null)`、`_setWisdomPaused(false)` 和 `_setResultMode(false)`，保证返回首页后的视觉状态与首次打开一致。

## 请保留的兼容代码

`public/shenshu.html` 仍保留八个旧场景渲染器及其字段结构。当前服务端会在旧字段之外增加统一摘要字段，前端先渲染统一四层摘要，再渲染原场景内容。后续若重构，需一次迁移一个场景并做浏览器回归，不要直接删除整段旧渲染器。

首页的 Three.js 星图、CSS 知识星球和名言轨道是三套叠加视觉，不是重复实现：

- Three.js canvas：实时天象、北斗、二十八宿、五行星、八卦和浑天仪。
- CSS `.wisdom-planet`：首页中央具有实体感的知识星球。
- DOM `.wisdom-quote`：可悬停、可暂停的环绕名言。

请不要为了“去重”直接删除其中任意一层。若调整视觉，应先在 `localhost:3000/shenshu.html` 对比首页、等待层、结果页和返回首页四个状态。

## 环境变量

- `ACCESS_TOKEN`：预览访问密码。
- `AUTH_SECRET`：签名密钥，生产环境必须设置为独立的长随机字符串。
- 其余 DeepSeek、智谱、Supabase 变量沿用现有配置。

## 已验证

- 8 个场景均完成真实接口烟测；修正后易经简要回答可完整解析。
- 简要示例约 599 字符 / 7.7 秒；同题深度约 1726 字符 / 21 秒。
- 登录返回 token 而非密码；无 token 的问答返回 401；第 7 次分钟内请求返回 429。
- 390x844 无横向溢出；桌面结果宽度 1040px。
- 浏览器控制台未见 Three.js 或其他警告。
- `npm run lint` 和 `npm run build` 通过。
- 2026-06-08 已在真实浏览器完成“输入问题 → 问天等待 → 结果显示 → 重新问天”完整回归。
- 等待层确认显示新的知识星球推演动画；结果页确认显示暗色旋转星球背景。
- 返回首页后 `resultPage`、`landing`、`body` 状态正确复原，28 条桌面名言恢复运行，星点尺寸未再次放大。
- 最新修改再次通过内联脚本语法检查、`npm run lint`、`npm run build` 和 `git diff --check`。

## 后续建议

- 将进程内限流替换为 Supabase/Redis 等共享存储，适配多实例部署。
- 为正常、空检索、模型异常、限流四类状态补自动化集成测试。
- 继续缩短 C、E、G 等场景的简要响应；当前模型和网络波动下偶尔仍会超过 15 秒。
- 生产部署前设置独立 `AUTH_SECRET`，并确认反向代理正确覆盖客户端 IP 头。
