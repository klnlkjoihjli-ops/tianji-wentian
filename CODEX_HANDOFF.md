# Claude Code 交接说明

更新时间：2026-06-08

## 本轮目标

将产品收敛为面向普通中文用户的“东方 AI 顾问”：更容易提问、更快得到可信回答、更容易采取行动，同时保留现有黑金天象视觉。

## 已完成

- 首页只直接展示 6 个核心分类，儒家与兵法收入“更多典籍”。
- 首页历史记录默认只展示 2 条，可展开其余记录。
- “简要 / 深度”重新可见，默认简要；两种模式的输出长度和耗时已有明显差异。
- 服务端统一返回 `advisor`、`sources`、`confidence`、`safetyNotice`。
- 结果页新增四层摘要：一句结论、典籍依据、现代解释、今日行动。
- 引用只展示服务端检索结果；无可靠检索时要求模型明确说明，不补造原文。
- 中医、时事、命理场景均增加对应的安全边界或不确定性提示。
- 意图分析、典籍检索和历史读取改为并行。
- 登录改为 8 小时有效的 HMAC 签名 token；密码不再返回浏览器；登录与问答均有限流。
- 移除浏览器端 DeepSeek 调用和重复 API key 逻辑。
- 修复 Three.js 线材质警告；小屏、低并发设备和减少动态效果偏好下自动降低粒子、像素比和帧率。
- 桌面结果宽度约 1040px；390px 手机端单列、无横向溢出，并隐藏日晷和八卦文字装饰。
- 首页视觉改为“知识星球”构图：中央暗金星球缓慢呼吸，名言沿三层椭圆轨道环绕运行。
- 环绕名言转到正面时自动变亮、放大，鼠标悬停时暂停并进一步放大；移动端自动减少名言数量。
- 缩小北斗、二十八宿、五行星及其光晕，移除原先大光斑感，保留精细星图和实时天象运动。
- “问天”等待层改为知识星球推演动画，显示“观象、索典、明理、成言”和分阶段进度。
- 结果页增加低对比度旋转星球水印，保持黑金风格，但不干扰正文阅读。
- “简要 / 深度”只保留在初始首页，结果页不再重复显示。
- 修复多次进入结果页时八卦、北斗材质透明度反复相乘的问题。
- 修复回答后点击“重新问天”，星点和光晕突然变大的问题；现在会恢复场景高亮、五行星缩放和名言轨道状态。
- 密码输入页升级为产品引导页：进入前会说明产品定位、典籍范围、四类核心能力和回答结构，不再只有孤立的密码框。

## 关键文件

- `src/lib/auth.ts`：token 签发与校验。
- `src/app/api/auth/route.ts`：登录和登录限流。
- `src/app/api/ask/route.ts`：并行检索、结构化结果、安全提示和问答限流。
- `src/lib/prompts.ts`：八个场景提示词及医疗边界。
- `src/app/page.tsx`：不再向 iframe URL 注入密码。
- `public/shenshu.html`：首页、结果层、认证客户端和动画降级。
- `CODEX_HANDOFF.md`：Codex 与 Claude Code 共用的交接记录；后续修改请继续追加到本文件。

## 2026-06-08 视觉与动画改造细节

主要改动均位于 `public/shenshu.html`：

- CSS 中新增 `.wisdom-planet`、`.planet-meridian`、`.wisdom-quote` 和 `.orbit-guide`，构成首页知识星球与三层名言轨道。
- `initFloatingQuotes()` 已替换为 `initWisdomOrbit()`。新逻辑使用单一 `requestAnimationFrame` 更新名言位置、景深、透明度和缩放，不再为每条名言生成独立随机 keyframes。
- `window._setWisdomPaused(on)` 用于在问天期间暂停名言轨道，返回首页后恢复。不要删除，否则隐藏页面中的动画仍会持续更新。
- Three.js 的北斗、二十八宿、五行星尺寸和光晕透明度已整体降低；八卦与浑天仪线条也同步降噪。
- `window._highlightScene(scene)` 不再依赖球体半径判断星点与光晕。星点缩小后旧判断会失效，导致返回首页时光晕被错误设置为高透明度。
- `baguaGroup` 的基础缩放是 `2.4`。动画循环必须围绕 `2.4` 轻微呼吸，不能重新写成围绕 `1` 缩放。
- `window._setResultMode(on)` 保存并按基础透明度计算八卦与北斗状态，不能使用 `opacity *= ...`，否则多次问天后会不断变暗。
- 等待层新增 `.loader-core` 和 `.loader-oracle`；旧的 `.div-hex` 暂时保留兼容，但已隐藏且 `showLoader()` 不再调用 `animateDivHex()`。
- 结果页通过 `#resultPage::after` 显示缓慢旋转的暗色星球水印。
- `goBack()` 会主动调用 `_highlightScene(null)`、`_setWisdomPaused(false)` 和 `_setResultMode(false)`，保证返回首页后的视觉状态与首次打开一致。

## 2026-06-08 登录引导页改造

改动位于 `public/shenshu.html` 的 `#loginGate`：

- 桌面端使用左右分栏。左侧介绍“东方典籍 AI 顾问”的定位，右侧保留密码入口。
- 产品文案明确说明“不是替你预言，而是陪你把问题看得更深”，避免把产品包装成确定性的占卜工具。
- 展示四类核心能力：看清局势、安顿内心、调养身心、辅助决策。
- 展示统一回答结构：一句结论、典籍依据、现代解释、今日行动。
- 登录卡片保留原来的 `lgInput`、`lgBtn`、`lgErr` ID 和 `doLogin()` 调用，因此认证逻辑没有改动。
- 背景新增低对比度天体轨道，密码卡片增加引文和安全提示，但没有引入图片或额外网络资源。
- 760px 以下自动切换单列；420px 以下隐藏能力卡片的次级说明，保证密码入口仍在手机首屏内。

## 请保留的兼容代码

`public/shenshu.html` 仍保留八个旧场景渲染器及其字段结构。当前服务端会在旧字段之外增加统一摘要字段，前端先渲染统一四层摘要，再渲染原场景内容。后续若重构，需一次迁移一个场景并做浏览器回归，不要直接删除整段旧渲染器。

首页的 Three.js 星图、CSS 知识星球和名言轨道是三套叠加视觉，不是重复实现：

- Three.js canvas：实时天象、北斗、二十八宿、五行星、八卦和浑天仪。
- CSS `.wisdom-planet`：首页中央具有实体感的知识星球。
- DOM `.wisdom-quote`：可悬停、可暂停的环绕名言。

请不要为了“去重”直接删除其中任意一层。若调整视觉，应先在 `localhost:3000/shenshu.html` 对比首页、等待层、结果页和返回首页四个状态。

## 环境变量

- `ACCESS_TOKEN`：预览访问密码。
- `AUTH_SECRET`：签名密钥，生产环境必须设置为独立的长随机字符串。
- 其余 DeepSeek、智谱、Supabase 变量沿用现有配置。

## 已验证

- 8 个场景均完成真实接口烟测；修正后易经简要回答可完整解析。
- 简要示例约 599 字符 / 7.7 秒；同题深度约 1726 字符 / 21 秒。
- 登录返回 token 而非密码；无 token 的问答返回 401；第 7 次分钟内请求返回 429。
- 390x844 无横向溢出；桌面结果宽度 1040px。
- 浏览器控制台未见 Three.js 或其他警告。
- `npm run lint` 和 `npm run build` 通过。
- 2026-06-08 已在真实浏览器完成“输入问题 → 问天等待 → 结果显示 → 重新问天”完整回归。
- 等待层确认显示新的知识星球推演动画；结果页确认显示暗色旋转星球背景。
- 返回首页后 `resultPage`、`landing`、`body` 状态正确复原，28 条桌面名言恢复运行，星点尺寸未再次放大。
- 最新修改再次通过内联脚本语法检查、`npm run lint`、`npm run build` 和 `git diff --check`。
- 新版登录页已在 1280x720 桌面和 390x844 手机视口检查；手机端无横向溢出，密码输入框底部约位于首屏 527px。
- 登录页改造后再次通过内联脚本语法检查、`npm run lint` 和 `git diff --check`。应用内浏览器的自动密码填充被其安全输入机制拦截，因此本轮未通过自动化重新提交密码，但认证 DOM ID 与事件处理函数均未修改。

## 后续建议

- 将进程内限流替换为 Supabase/Redis 等共享存储，适配多实例部署。
- 为正常、空检索、模型异常、限流四类状态补自动化集成测试。
- 继续缩短 C、E、G 等场景的简要响应；当前模型和网络波动下偶尔仍会超过 15 秒。
- 生产部署前设置独立 `AUTH_SECRET`，并确认反向代理正确覆盖客户端 IP 头。

## 2026-06-19 小修复交接清单（给 Claude Code）

本轮只做小修复，不要大幅改结构、不要重写 `public/shenshu.html`、不要删除现有场景渲染器或视觉层。优先修“内容已经存在但用户触发不到”的问题。

### P0：修复易经/起卦入口误判

实测线上输入：

`用易經起一卦，問我下半年事業應該如何取捨？`

当前会返回 `scene: "A"`，走到“推背·命理”，引用《增广贤文》《论语》等；但强制传 `scene: "D"` 时，12.9 秒内能正确返回本卦、动爻、变卦、小六壬。因此底层起卦链路是好的，问题在场景识别。

需要同步改两处：

- `public/shenshu.html` 的 `detect(q)`
- `src/lib/prompts.ts` 的 `detectScene(q)`

把易经起卦正则从目前的窄匹配：

```js
/起卦|占卦|卦象|搖卦|易經問/
```

扩展为至少覆盖：

```js
/易經|易经|周易|起卦|占卦|占問|占问|卦象|卦辭|卦辞|爻辭|爻辞|動爻|动爻|變卦|变卦|搖卦|摇卦|六爻|梅花易數|梅花易数|小六壬/
```

注意前端是繁体页面，服务端是简体代码，两边都要同时覆盖繁简关键词。修完后不要改 `searchClassics()` 的 D 场景起卦逻辑，那里目前工作正常。

验收：

- 不点分类，只输入“用易經起一卦……”应返回 `scene: "D"`。
- Sources 应包含 `起卦 · 本次起卦结果`、`易经 · XX（本卦）`、`易经 · XX（变卦）` 或小六壬。
- `npm run lint`、`npm run build` 通过。

### P1：避免前端误判强制压过服务端

当前 `public/shenshu.html` 会把前端检测出的 `scene` 放进 `/api/ask` 请求体。这样一旦前端误判，服务端就没有机会重新识别。

小改即可，不要重构接口：

- 若用户明确点击了分类按钮，继续传 `pinnedScene`。
- 若用户没有点击分类，只是自然输入，可以考虑不传 `scene`，让服务端 `detectScene(question)` 做最终判断。
- 如果暂时不想改请求结构，至少先完成 P0，让前后端正则一致。

验收：

- 点击“易經 · 起卦”仍能强制进入 D。
- 自然输入“周易帮我看看选择”“六爻问事业”也能进入 D。
- 兵法、儒家、中医、节气、道家、时事原有入口不回退。

### P1：把新增典籍轻量露出到“更多典籍”

数据库已有约 2034 条典籍，易经、梅花易数、小六壬、六爻、道教、孟子、荀子等内容已经很厚，但首页“更多典籍”目前只露出“儒家 · 修身”和“謀略 · 兵法”，用户看不出新增内容。

只做轻量 UI，不要新增大栏目：

- “更多典籍”里增加 3-5 个小按钮即可，例如：
  - `易傳 · 義理`
  - `梅花 · 六爻`
  - `小六壬 · 速斷`
  - `道教 · 清修`
  - `諸子 · 百家`
- 按钮可以复用现有 `pill` 样式和 `pinnedScene`/`sq()` 逻辑，不要新增复杂动画。
- 如果新增按钮最终仍走 D/F/G/H 等现有场景即可，不需要新增第九场景。

验收：

- 首页视觉不拥挤。
- 390px 移动端无横向溢出。
- 点击新增按钮能填入对应示例问题，并进入合理场景。

### P1：确认生产环境变量

本地 `.env.local` 当前可见问题：

- `AUTH_SECRET` 未设置；代码会退回用 `ACCESS_TOKEN` 做 HMAC 签名密钥。
- `NEXT_PUBLIC_APP_URL` 不是生产域名。

不要在代码里硬编码密钥。到 Vercel 环境变量确认：

- `AUTH_SECRET`：必须是独立长随机字符串，不要等于访问密码。
- `NEXT_PUBLIC_APP_URL`：建议设为 `https://tianji.zhangkengkeng.cn`。
- `ZHIPU_API_KEY`、`DEEPSEEK_API_KEY`、Supabase 相关变量仍需保持存在。

验收：

- 登录仍返回短期 token 和 `expiresAt`。
- `/api/ask` 携带 token 正常，无 token 返回 401。

### P2：更新过期审查文档

`docs/review-issues.md` 里有些旧问题已经过期：

- `feedback` 表已经存在，Supabase 实测 count 为 3。
- `scripts/data/` 现在已有大量数据文件，不再是空目录。

可把旧问题标记为“已修复/待复核”，避免后续重复排查。不要删除整份文档，保留历史背景即可。

### 修复前实测记录

- 线上自然输入“用易經起一卦……”：返回 `scene: "A"`，耗时约 17.2 秒，这是待修 bug。
- 线上强制 `scene: "D"`：返回 `scene: "D"`，耗时约 12.9 秒，起卦内容正确。
- Supabase `classics` 表总数约 2034 条；主要来源包括《论语》210、《易经》151、《荀子》107、《孙子兵法》99、《道德经》81、《黄帝内经》81。
- `npm run lint` 通过。
- `npm run build` 通过。

### 2026-06-19 修复结果

- 已修复 P0：`public/shenshu.html` 与 `src/lib/prompts.ts` 的易经/起卦关键词已同步扩展，覆盖 `易經/易经/周易/六爻/梅花易數/小六壬/动爻/变卦` 等常见说法。
- 已修复 P1：自然输入时前端不再把猜测出的 `scene` 强制传给 `/api/ask`；只有用户明确点击分类按钮时才强制传场景。这样服务端 `detectScene(question)` 能做最终判断。
- 已修复 P1：`更多典籍` 轻量增加 `易傳 · 義理`、`梅花 · 六爻`、`小六壬 · 速斷`、`道教 · 清修`、`諸子 · 百家`，复用现有 `pill` 样式和八场景逻辑，没有新增场景。
- 已更新 P2：`docs/review-issues.md` 中 `feedback` 表缺失、典籍数据文件未入库两个旧问题已标记为 2026-06-19 复核已修复。
- 已验证：本地 `/api/ask` 不传 `scene`，自然输入“用易經起一卦……”返回 `scene: "D"`，耗时约 13.1 秒；sources 包含 `起卦 · 本次起卦结果`、`易经 · 雷水解（本卦）`、`易经 · 雷风恒（变卦）`、`小六壬 · 小吉`。
- 已验证：`detectScene()` 对 `易經/周易/六爻/小六壬` 返回 D，对兵法问题返回 H，对内经失眠问题返回 B。
- 已验证：`npm run lint`、`npm run build`、`git diff --check` 均通过。

### 2026-06-19 起卦优先级二次修复

- 问题原因：虽然 `易經/周易/起卦` 已能进入 D，但 `我想算一卦`、`帮我算卦`、`求卦问感情` 等常用说法没有覆盖，会掉到默认 A；同时 `2026/局势/趋势` 等时事词在识别顺序上早于 D，某些“起卦看 2026...”也可能被 C 抢走。
- 已调整：`public/shenshu.html` 与 `src/lib/prompts.ts` 中，明确的易经/起卦/算卦/周易/六爻/梅花/小六壬关键词现在排在时事识别之前。
- 已新增关键词：`算卦`、`算一卦`、`求卦`、`问卦/問卦`、`卜卦`、`占一卦`。
- 已验证：`起卦看2026事业`、`易经问今年全球局势`、`我想算一卦`、`帮我算卦`、`求卦问感情` 均返回 D；纯 `今年全球局勢與中國機遇` 仍返回 C；纯 `今年大勢如何` 仍返回 A。
- 已验证：`npm run lint`、`npm run build`、`git diff --check` 均通过。
- 仍需人工确认：Vercel 生产环境是否设置独立 `AUTH_SECRET`，且 `NEXT_PUBLIC_APP_URL` 是否为 `https://tianji.zhangkengkeng.cn`。不要在代码里硬编码这些变量。

### 2026-06-19 输入区清晰度优化

- 已把首页输入区改成“主输入优先”：新增说明 `直接把你的問題寫下來`，明确告诉用户“不必先选分类，系统会自动判断”。
- 已把六个主分类改成“可选方向”，每个按钮增加一行用途说明，例如 `看趨勢與階段`、`睡眠體質作息`、`取捨進退決策`。这样分类更像快捷示例，而不是必须理解的复杂栏目。
- `更多典籍` 改为 `更多典籍方向`，仍默认折叠，避免新增典籍按钮压迫首屏。
- 移动端隐藏按钮副说明，只保留短标签；桌面显示用途说明。390x844 实测无横向溢出，首屏可完整看到主输入与六个方向。
- 已从 git `HEAD` 导出本轮修改前的首页备份：`backups/shenshu.before-current-uncommitted.html`。该文件用于对比或恢复，不会覆盖当前 `public/shenshu.html`。
- 已验证：`npm run lint`、`npm run build`、`git diff --check` 均通过。

### 2026-06-20 首页与来源可信度小幅升级（Codex）

本轮遵循“小修复、不重写”的原则，未删除任何场景渲染器、典籍数据或星球动画。

#### 修改前备份

- Git 备份分支：`backup/before-codex-upgrade-20260619-235416`
- 对应提交：`6452a810ae468bd8f08cea3db58de7073f7548c5`
- 独立首页副本：`backups/shenshu.before-upgrade-20260619-235416.html`

#### 已完成修改

- 首页八个方向固定为桌面 `4×2`，保持现有黑金星球视觉，不新增栏目。
- 首页说明改为“可以先选方向，也可以直接提问”，消除“先选”与“可选”的文案冲突。
- 方向卡由带 `onclick` 的 `div` 改为真正的 `button`，加入 `aria-pressed` 和键盘焦点样式；“改为自动判断”也改为按钮。
- `推背·命理` 统一更名为 `推背·大势`；推背示例改为行业趋势、事业阶段和顺势进退，不再出现属相、婚姻、犯太岁等不匹配内容。
- 修复结果页顶部切换场景：现在调用 `renderScene(k, lastQ, true)`，明确把用户选择的新场景传给服务端。旧逻辑只换前端标签，服务端可能仍按原问题自动分类。
- 易经摘要主来源不再显示 `起卦 · 本次起卦结果`，会显示到具体本卦《象传》，例如 `《易經》 · 泽风大过 · 象傳`。
- 服务端新增“按实际引文反查检索原文”的来源匹配，优先于模型返回的 `classicRef`；解决引用《论语》却误标《增广贤文》的问题。
- 简要模式生成上限从 `1300` 收紧到 `1100` tokens；结构化生成温度从 `0.88` 降为 `0.68`，降低跑题、JSON 不闭合和重试概率。深度模式上限未改。
- 等待层改为显示真实“已等待 N 秒”，不再显示经常失准的倒计时。

#### 浏览器回归

- 1280×720：首页方向卡稳定为 `4×2`，无横向溢出，控制台无错误或警告。
- 390×844：八个方向仍在首屏内，无横向溢出，输入框和历史记录可见。
- 选择“易经·起卦”后，按钮 `aria-pressed=true`，并显示可键盘操作的“改为自动判断”。
- 起卦实测：主来源正确显示 `《易經》 · 泽风大过 · 象傳`，详细来源含本卦、彖传、变卦和小六壬。
- 结果页从“易经·起卦”切换到“儒家·修身”，服务端实际返回 `scene: G`，不再沿用 D 场景。
- 儒家实测：引用“知止而后有定……”时，主来源正确显示 `《大学》 · 大学`。
- `npm run lint`、`npm run build`、`git diff --check` 均通过。

#### 仍需继续观察

- 简要回答在模型和网络波动下仍可能超过 15 秒。本轮只做了保守的长度与稳定性优化，没有更换模型、删减旧场景字段或改变典籍检索结构；后续应记录服务端检索耗时、首 token 时间和完整生成时间，再决定是否拆分首屏摘要与详细内容。
- 本轮修改尚未提交或推送。继续前先查看 `git status`，不要覆盖这批未提交修改。

### 2026-06-20 名言轨道性能优化与罗盘移除

- 用户反馈首页环绕名言转动时有卡顿。主要原因是桌面端 28 个 DOM 文本在每一帧同时更新 `transform`、`opacity`、`z-index`、`filter: blur()` 和前景 class，并与 Three.js 星空同时渲染；动态文字模糊会持续触发昂贵的重绘。
- 已移除名言的逐帧 `filter: blur()`，保留透明度、景深缩放、前景放大和悬停高亮，因此视觉构想没有删除。
- 桌面名言数量从 28 调整为 24；低性能桌面为 16；700px 以下手机为 8。
- 名言轨道独立限制为约 30fps，低性能设备约 25fps；缓存视口尺寸、层级和前景状态，只有变化时才写入对应样式/class。
- 当问天、悬停或系统开启“减少动态效果”导致轨道暂停时，只补绘一次静态状态，之后不再持续写 DOM。
- 已删除左下角动态创建的 `#sundial`“十二时辰仪”及其每分钟更新逻辑；节气场景内部的节气罗盘没有删除。
- 浏览器验证：1280×720 为 24 条名言，390×844 为 8 条；两种视口均无横向溢出，`#sundial` 不存在，轨道位置会持续更新，控制台无错误或警告。
- `npm run lint`、`npm run build`、`git diff --check` 均通过。

### 2026-06-20 首页交互改版 + 全站动效升级（Claude Code，用 frontend-design 技能）

本轮主题色与黑金天象视觉**完全不变**，只升级交互结构与动效质感。所有改动集中在 `public/shenshu.html`，未触碰 Three.js 星图、`baguaGroup` 缩放、`_setResultMode`、`searchClassics` 起卦逻辑或检索内核。

#### 首页：改为「选优先 + 兜底」两步式，方向卡收敛为 8 个

- 首页结构改为两步：① 选择方向（可选），② 写下你的问题。
- 之前「更多典籍方向」里有冗余按钮（起卦类 4 个、道家/儒家各 2 个都 funnel 到同一场景），已**收敛为 8 张方向卡 = 8 个真实场景（A–H）**，全部平铺、不再折叠；副说明体现各方向典籍范围（如起卦→「起卦·變卦·小六壬」、道家→「老莊·清靜·心法」）。
- 新增 `selectDirection(scene,el)` / `clearDirection()`：点方向＝钉住 `pinnedScene` + 高亮卡片 + 切换该方向输入提示语（`SCENE_PLACEHOLDERS`）+ 出示例，**不自动提交**；未选方向则走服务端 `detectScene` 兜底；选后显示「已选方向 · 改为自动判断」可一键回退。
- `goBack()` 末尾调用 `clearDirection()` 复位选择。

#### 全站动效升级（纯 CSS + 极少 JS）

关键约束（改视觉时请保留）：

- 入场动画统一用 `@keyframes fuRise` + `animation-fill-mode: backwards`，**不要用 forwards/both**，否则动画结束后会锁死 transform、破坏卡片 hover 抬升。
- 选中态辉光 `.pill.pill-on{animation:pillSelGlow ... !important}` 必须保留 `!important`，否则会被更高优先级的入场 `nth-child` 规则（`#landing.landing-reveal .pills .pill`）盖掉。
- 入场只在登录后首屏播放一次：登录成功（两条认证路径）调用 `revealLanding()` 给 `#landing` 加 `landing-reveal` 类，动效全部 scope 在 `#landing.landing-reveal` 下；不要改成页面 load 时无条件播放，否则会被登录门挡住看不到。
- 所有动效在 `@media(prefers-reduced-motion:reduce)` 下降级为静态。

各环节动效：

- **登录页**：`#loginGate` 标题点燃（`igniteTitle`）+ 介绍栏/四能力卡/密码卡错落浮现；`#loginGate.hide` 时禁用入场动画避免与淡出冲突。
- **首页**：天機标题点燃 → 副标题/步骤标签/8 卡（nth-child 错落）/输入框依次浮现；方向卡悬停金色光带扫过（`pillSheen`）+ 抬升，选中态金边呼吸辉光（`pillSelGlow`）+ 右上角金点；输入框 `:focus-within` 金环绽放、问天按钮轻脉冲。
- **等待层 loader**：打开时分层绽放；核心星球叠加缓转金色经线弧光（`.loader-core::after` conic + mask）；四象「觀象→索典→明理→成言」依次点亮（`oracleGlow` staggered）；进度条流动金光（`barSheen`）+ 激活步骤发光。未改 `setProgress` 逻辑。
- **结果页**：`#resultPage.show` 下 `.hero-frame` → `.hero-quote` → `.hero-source` → `.hero-confidence` → `.hero-conclusion` → `.hero-actions` 逐层揭示（`revealUp`），展开区 `.hero-field` 同样错落。

#### 验证

- 本地预览（1280×800 / 375×812）：登录页、首页入场、方向卡 hover/选中、输入聚焦、loader 四象轮亮、起卦端到端（水山蹇本卦+变卦+小六壬+象传来源）全部正常，无控制台报错（除测试时用假 token 触发的 401，与本轮无关）。
- 移动端无横向溢出；`npm run build` 通过；已部署生产。

#### 后续可做（未做）

- `frontend-design`、`theme-factory` 等设计技能已安装；如需更大幅度视觉重做，可在新会话中调用。
- 起卦结果页可考虑为「摇卦→本卦/变卦逐爻画线」做一段专属揭示动画（目前是统一 `revealUp`）。
